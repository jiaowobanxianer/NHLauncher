<Window x:Class="LauncherPacker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LauncherPacker"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Left">
            <Menu >
                <MenuItem Header="文件">
                    <MenuItem Header="新建项目" Click="CreateProject"/>
                    <MenuItem Header="打开项目" Click="OpenProject"/>
                    <MenuItem Header="保存项目" Click="SaveProject"/>
                </MenuItem>
            </Menu>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Center">
            <TextBox x:Name="FolderPathTextBox" 
                 Margin="0,10,0,0"
                 HorizontalAlignment="Left"
                 Width="auto"
                 MinWidth="200"
                 Height="30" 
                 IsReadOnly="True"
                 TextChanged="FolderPathTextBox_TextChanged" 
                 ToolTip="选择的文件夹路径"/>
            <Button x:Name="SelectFolderButton" 
                 HorizontalAlignment="Right"
                 Content="选择文件夹" 
                 Margin="0,10,0,0"
                 Click="SelectFolderButton_Click"/>
        </StackPanel>
        <StackPanel Grid.Row="2" >
            <TextBlock Text="远程服务器路径：" 
               HorizontalAlignment="Left" 
               Margin="0,10,0,0"/>
            <TextBox x:Name="RemoteURLText" 
         Margin="0,10,0,0"
         HorizontalAlignment="Left"
        
         Width="350" 
         Height="30" 
         TextChanged="RemoteURLText_TextChanged"
         ToolTip="输入远程服务器路径"/>
        </StackPanel>
        <Button x:Name="GenerateManifest" 
                Grid.Row="3" 
                Content="生成Manifest" 
                Width="120" 
                Margin="80,20,80,0"
                Height="30"
                Click="GenerateManifestButton_Click"/>
        <StackPanel Grid.Row="4">
            <TextBlock x:Name="ProgressTextBlock" 
               HorizontalAlignment="Left" 
               Margin="0,10,0,0" 
               Text="上传进度：0%"/>
            <ProgressBar HorizontalAlignment="Left"  Height="10" Margin="71,10,0,0" VerticalAlignment="Center" Width="294" x:Name="progressBar"/>
        </StackPanel>
        <Button x:Name="UploadButton" 
        Grid.Row="5" 
        Content="上传" 
        Width="120" Margin="130,20,130,-9"
        Click="Upload"/>
    </Grid>
</Window>
